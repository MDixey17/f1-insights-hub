import { Constructor } from "./Constructor"

export interface ConstructorStandings {
  _attributes: {
    position: string
    positionText: string
    points: string
    wins: string
  }
  Constructor: Constructor
}
